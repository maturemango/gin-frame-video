[Unit]
Descriptoon=Gin Frame Service
After=network.target

[Service]
Type=simple
##ExecStart为可执行编译文件所在路径
ExecStart=../../gin-frame
Restart=always
RestartSec=1
StartLimitInterval=100
Environment="GIN_MODE=release"

[Install]
WantedBy=multi-user.target